{
  "name": "axante-website-preview",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/assert-build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
