{
  "name": "axante-website-preview",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-home.mjs && node scripts/generate-pages.mjs && node scripts/publish-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
